name: Run linters and tests on push

on: ["push"]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Hello, this is step 1!"
      
      - name: Step 2
        run: echo "Hello, and this is step 2!"